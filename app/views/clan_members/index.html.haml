.text-content
  .row
    .columns
      %h2 Members:

      %ol
        - @clan_members.each do |clan_member|
          %li
            = clan_member.username
            = link_to 'Profile', clan_member_path(clan_member)
            = clan_member.steam_link_tag
