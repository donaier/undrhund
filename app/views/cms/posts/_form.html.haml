= "von: #{resource.clan_member.username}, am: #{l(resource.created_at)}"
%hr
= simple_form_for([:cms, resource.topic, resource], wrapper: :bootstrap, html: { class: 'form-horizontal' } ) do |f|
  = f.error_notification

  .form-inputs
    = f.input :text, input_html: { rows: '15', style:  "width:80%" }

  .form-actions
    = f.button :submit
