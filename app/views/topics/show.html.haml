.board
  .row
    .columns

  .text-content.topic
    .row
      .columns
        %h2= @topic.title
    .row
      .columns.large-9
        %p= @topic.text
      .columns.large-3.infobox
        %p= @topic.clan_member.username
        %p= @topic.created_at.strftime("%B %d, %Y - %H:%M")

  - if @topic.posts.any?
    .text-content.post
      - @topic.posts.each do |post|
        .row
          .columns.large-9
            %p= post.text
          .columns.large-3.infobox
            %p= post.clan_member.username
            %p= post.created_at.strftime("%B %d, %Y - %H:%M")

  .text-content.post-form
    .row
      = simple_form_for @post, url: topic_posts_path(@post.topic.id) do |f|
        .columns.large-9
          = f.input :text, label: false
          = f.hidden_field :topic_id
          = f.hidden_field :clan_member_id
        .columns.large-3.infobox
          = f.submit 'Post!'
